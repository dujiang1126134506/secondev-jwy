description = "E10 集成企业微信日程（会议提醒）二开模块"

// 后端二开打包规范（文档 2.6.1）：排除 resources 目录。
// 企业微信配置由 E10 配置中心的 weaver-secondev-wecom.properties 提供。
sourceSets {
    main {
        resources {
            exclude '**'
        }
    }
}

dependencies {
    // 子项目私有依赖添加（fastjson / httpclient 已由根项目 secDevLib 提供，无需重复声明）
}
